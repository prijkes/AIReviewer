# ============================================================================
# Azure DevOps Provider - Environment Variables
# ============================================================================
#
# This file contains Azure DevOps-specific environment variables.
# Copy this file to azure.env and fill in your actual values.
#
# IMPORTANT:
#   - Never commit azure.env to version control (it's in .gitignore)
#   - These settings are only required when using the Azure DevOps provider
#   - For generic environment variables, see .env.example in the root
#
# ============================================================================

# ----------------------------------------------------------------------------
# Azure DevOps Connection
# ----------------------------------------------------------------------------

# Azure DevOps organization URL
# Example: https://dev.azure.com/myorg
ADO_COLLECTION_URL=https://dev.azure.com/your-organization

# Azure DevOps project name
ADO_PROJECT=YourProject

# Azure DevOps repository ID (GUID) - either this or ADO_REPO_NAME
# Find in: Azure DevOps → Project Settings → Repositories → Repository → Properties
ADO_REPO_ID=00000000-0000-0000-0000-000000000000

# OR Azure DevOps repository name - either this or ADO_REPO_ID
# ADO_REPO_NAME=MyRepository

# Pull request ID to review (optional - can be inferred from BUILD_SOURCE_VERSION)
# ADO_PR_ID=123

# Azure DevOps Personal Access Token (PAT)
# Requires: Code (Read & Write), Pull Request Threads (Read & Write)
# Create at: https://dev.azure.com/{organization}/_usersSettings/tokens
ADO_ACCESS_TOKEN=your-pat-token-here

# Build source version (commit SHA) for inferring PR
# Automatically set in Azure Pipelines as $(Build.SourceVersion)
# BUILD_SOURCE_VERSION=abc123def456

# ----------------------------------------------------------------------------
# Local Repository Path
# ----------------------------------------------------------------------------

# Path to local cloned repository
# This dramatically reduces API calls and avoids rate limiting
# In Azure Pipelines, set to: $(Build.SourcesDirectory)
# For local development, set to your repo clone path
LOCAL_REPO_PATH=/path/to/local/repo

# ----------------------------------------------------------------------------
# Azure Service Bus (for queue-based operation)
# ----------------------------------------------------------------------------

# Azure Service Bus connection string
# Get from: Azure Portal → Service Bus Namespace → Shared access policies
# Must have Manage, Send, and Listen permissions
AZURE_SERVICEBUS_CONNECTION_STRING=Endpoint=sb://your-namespace.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-key-here


# ============================================================================
# End of Azure DevOps Provider Environment Variables
# ============================================================================
